<div class="container mt-4">
    <div class="row">
        <div class="col-sm">
            <div class="card" style="width: 18rem;">
                <img class="card-img-top" src="{{product.image}}" alt="Card image" style="width:100%">
                <div class="card-body">
                    <h4 class="card-title">{{product.trademark}}</h4>
                    <p class="card-text">{{product.name}}</p>
                    <p class="my-2 text-danger">{{product.price}}VNĐ</p>
                    <a href="/product/create" class="btn btn-primary">Add to cart</a>
                </div>
            </div>
        </div>
    </div>
</div>